name: hive_project
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.2

  hive: ^2.0.4
  hive_flutter: ^1.1.0
  path_provider: ^2.0.3

dev_dependencies:
  hive_generator: ^1.1.0
  build_runner: ^2.1.2
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
